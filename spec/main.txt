// RESOURCE: https://www.cs.uic.edu/~jbell/CourseNotes/OperatingSystems/12_FileSystemImplementation.html
<- WE USE UNSIGNED INTEGERS BECAUSE YOU CAN'T HAVE NEGATIVE INDEX ->

>---- HEADER  ----<
MAGIC: [u8; MAGIC_LENGTH]

FLAGS: u16, SPECIFIES;
	CHARACTER ENCODING
	ARCHIVE VERSION

CONTENT_VERSION: u16

USES_COMPRESSION: bool

>---- REGISTRY ( 176 * ENTRY_COUNT ) + 240 BYTES -----<
ENTRY_COUNT: u64

ENTRY:  * ENTRY_COUNT
	IS_SIGNED: bool
	SIGNATURE: [u8; SIGNATURE_LENGTH]

	PATH_NAME_START: u32
	PATH_NAME_END: u32

	INDEX_OF_LOCATION: u32
	BYTE_OFFSET: u64 ( SIZE OF BINARY DATA IN BYTES )

	MIME_TYPE: u16

>---- PAGES ( BINARY GLOB )  ----<
	∞ * usize