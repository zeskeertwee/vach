// RESOURCE: https://www.cs.uic.edu/~jbell/CourseNotes/OperatingSystems/12_FileSystemImplementation.html
<- WE USE UNSIGNED INTEGERS BECAUSE YOU CAN'T HAVE NEGATIVE INDEX ->

>---- HEADER  ----<
MAGIC: [u8; MAGIC_LENGTH]
ARCHIVE_VERSION: u16
FLAGS: u8
CAPACITY: u16

FLAGS: u16, SPECIFIES;
	CHARACTER_ENCODING
	ARCHIVE_VERSION
	USES_COMPRESSION

>---- REGISTRY -----<
ENTRIES: (x) * HEADER::NUMBER_OF_ENTRIES
	FLAGS: u8
	FLAGS: u16, SPECIFIES:
		USES_COMPRESSION
		IS_SIGNED

	SIGNATURE: [u8; SIGNATURE_LENGTH]

	PATH_NAME_START: u32
	PATH_NAME_END: u32

	INDEX_OF_LOCATION: u32
	BYTE_OFFSET: u64 ( SIZE OF BINARY DATA IN BYTES )

	MIME_TYPE: u16

>---- LEAFS ( BINARY GLOB )  ----<
∞ * usize
INCLUDES:
	PATH_NAMES
	BINARY_DATA

NB:
	Do NOT CREATE A NEW BufReader every time a Resource is loaded
	Switch from Vec to Box<[]>
	Use iterators when possible